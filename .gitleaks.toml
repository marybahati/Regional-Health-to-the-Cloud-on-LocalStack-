title = "gitleaks config"

[extend]
useDefault = true

[allowlist]
  description = "assignment evidence placeholders — not live credentials"
  paths = [
    '''evidence/''',
    '''iam/''',
    '''\.env$''',
    '''\.env\.local$''',
    '''.*\.pem$''',
    '''ca\.pem''',
  ]
